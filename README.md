# Stock Broker Dashboard

Streamlit-based web dashboard that allows users to:
- Login using email
- Subscribe/unsubscribe to stocks
- View live stock prices
- View a single combined live chart
- Supports multiple users asynchronously

## How to Run
```bash
pip install -r requirements.txt
Run : python -m streamlit run app.py
